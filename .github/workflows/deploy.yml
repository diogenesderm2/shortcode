name: Deploy Shortcode

on:
  push:
    branches: [main, stage, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo Building the app

  #deploy em desenvolvimento
  deploy-development:
    runs-on: ubuntu-latest
    environment:
      name: development
      url: ${{vars.URL}}
    needs: build
    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4
      - name: Shortcode Deployment
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{secrets.SSH_PRIVATE_KEY}}
          ARGS: "-rlgoDzvc --delete"
          SOURCE: "/var/www/shortcodeDev"
          remoteHost: ${{secrets.SSH_HOST}}
          remoteUser: ${{secrets.SSH_USER}}
          remotePort: ${{vars.REMOTE_PORT}}
          target: ${{vars.TARGET}}

  #deploy em stage
  deploy-stage:
    runs-on: ubuntu-latest
    environment:
      name: stage
      url: ${{vars.URL}}
    needs: deploy-development
    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4
      - name: Shortcode Deployment
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{secrets.SSH_PRIVATE_KEY}}
          ARGS: "-rlgoDzvc --delete"
          SOURCE: "/var/www/shortcodeStage"
          remoteHost: ${{secrets.SSH_HOST}}
          remoteUser: ${{secrets.SSH_USER}}
          remotePort: ${{vars.REMOTE_PORT}}
          target: ${{vars.TARGET}}

  #deploy em production
  deploy-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: ${{vars.URL}}
    needs: deploy-stage
    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4
      - name: Shortcode Deployment
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{secrets.SSH_PRIVATE_KEY}}
          ARGS: "-rlgoDzvc --delete"
          SOURCE: "/var/www/shortcode"
          remoteHost: ${{secrets.SSH_HOST}}
          remoteUser: ${{secrets.SSH_USER}}
          remotePort: ${{vars.REMOTE_PORT}}
          target: ${{vars.TARGET}}
